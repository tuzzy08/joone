import { StoreBackend, CompositeBackend, FilesystemBackend } from "deepagents";
console.log("StoreBackend:", typeof StoreBackend);
console.log("CompositeBackend:", typeof CompositeBackend);
console.log("FilesystemBackend:", typeof FilesystemBackend);
